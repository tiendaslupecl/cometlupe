SHOP_DOMAIN=0viv7s-w8.myshopify.com
ADMIN_API_TOKEN=PEGA_AQUI_TU_NUEVO_TOKEN

# Opcional: URL publica de la tienda (para submenus con filtros en el menu por GraphQL).
# Si no la pones, el script intenta leerla de Shopify.
# STORE_PRIMARY_URL=https://www.tiendaslupe.cl

# Permisos necesarios en la app (Admin API):
# - Productos, contenido, etc. (como ya tenes)
# - Para el MENU: "Online store navigation" lectura y escritura
#   (en la API: read_online_store_navigation, write_online_store_navigation)
# Si el menu fallo con 403, entra a la app en Shopify, agrega esos permisos, guarda
# y "Instalar de nuevo" la app. Puede pedirte un token nuevo.

# --- Otros proyectos en este repo (solo si los usas) ---
# SHOPIFY_API_KEY=your_api_key_here
# SHOPIFY_API_SECRET=your_api_secret_here
# SHOPIFY_API_SCOPES=read_products
# PORT=3000
# NODE_ENV=development
